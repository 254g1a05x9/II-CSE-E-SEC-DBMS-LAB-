
--q1
CREATE TABLE DEPT
(
    DNO NUMBER,
    DNAME VARCHAR2(30)
);

--q2
ALTER TABLE DEPT
ADD CONSTRAINT DEPT_PK PRIMARY KEY (DNO);

ALTER TABLE DEPT
MODIFY DNAME NOT NULL;

--q3
CREATE TABLE STUDENT
(
    SID NUMBER,
    SNAME VARCHAR2(30),
    DID NUMBER
);

--q4
ALTER TABLE STUDENT
ADD CONSTRAINT STUDENT_PK PRIMARY KEY (SID);

ALTER TABLE STUDENT
MODIFY SNAME NOT NULL;

ALTER TABLE STUDENT
ADD CONSTRAINT STUDENT_FK
FOREIGN KEY (DID) REFERENCES DEPT(DNO);


--q5
INSERT INTO DEPT VALUES (10, 'CSE');
INSERT INTO DEPT VALUES (20, 'ME');
INSERT INTO DEPT VALUES (30, 'CE');
INSERT INTO DEPT VALUES (40, 'EEE');
INSERT INTO DEPT VALUES (50, 'ECE');
INSERT INTO DEPT VALUES (60, 'CSM');
INSERT INTO DEPT VALUES (70, 'CSD');

COMMIT;

--q6
INSERT INTO STUDENT VALUES (101, 'Rahul', 10);
INSERT INTO STUDENT VALUES (102, 'Sneha', 20);
INSERT INTO STUDENT VALUES (103, 'Arjun', 30);
INSERT INTO STUDENT VALUES (104, 'Kiran', 40);
INSERT INTO STUDENT VALUES (105, 'Priya', 50);
INSERT INTO STUDENT VALUES (106, 'Nikhil', 10);
INSERT INTO STUDENT VALUES (107, 'Anjali', 60);
INSERT INTO STUDENT VALUES (108, 'Ravi', 30);
INSERT INTO STUDENT VALUES (109, 'Ayesha', 70);
INSERT INTO STUDENT VALUES (110, 'Vijay', NULL);

COMMIT;

--q7
SELECT * FROM STUDENT
NATURAL JOIN
(
    SELECT DNO AS DID, DNAME
    FROM DEPT
);

--q8
SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
INNER JOIN DEPT D
ON S.DID = D.DNO;

--q9
SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
JOIN DEPT D
ON S.DID > D.DNO;

-- Q10: LEFT OUTER NATURAL JOIN

SELECT S.SID, S.SNAME, S.DID, D.DNAME
FROM STUDENT S
LEFT OUTER JOIN
(
    SELECT DNO AS DID, DNAME
    FROM DEPT
) D
ON S.DID = D.DID;


-- Q11: RIGHT OUTER NATURAL JOIN

SELECT S.SID, S.SNAME, S.DID, D.DNAME
FROM STUDENT S
RIGHT OUTER JOIN
(
    SELECT DNO AS DID, DNAME
    FROM DEPT
) D
ON S.DID = D.DID;


-- Q12: FULL OUTER NATURAL JOIN

SELECT S.SID, S.SNAME, S.DID, D.DNAME
FROM STUDENT S
FULL OUTER JOIN
(
    SELECT DNO AS DID, DNAME
    FROM DEPT
) D
ON S.DID = D.DID;


-- Q13: LEFT OUTER EQUI JOIN

SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
LEFT OUTER JOIN DEPT D
ON S.DID = D.DNO;



-- Q14: RIGHT OUTER EQUI JOIN

SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
RIGHT OUTER JOIN DEPT D
ON S.DID = D.DNO;




-- Q15: FULL OUTER EQUI JOIN
SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
FULL OUTER JOIN DEPT D
ON S.DID = D.DNO;

-- Q16: LEFT OUTER CONDITIONAL JOIN
SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
LEFT OUTER JOIN DEPT D
ON S.DID > D.DNO;




-- Q17: RIGHT OUTER CONDITIONAL JOIN

SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
RIGHT OUTER JOIN DEPT D
ON S.DID > D.DNO;

-- Q18: FULL OUTER CONDITIONAL JOIN

SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
FULL OUTER JOIN DEPT D
ON S.DID > D.DNO;

-- Q19: CROSS JOIN

SELECT S.SID, S.SNAME, D.DNO, D.DNAME
FROM STUDENT S
CROSS JOIN DEPT D;
